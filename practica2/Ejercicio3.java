package practica2;

//import java.util.Scanner;

public class Ejercicio3 {
   /*
   public static Restaurante restauranteMásPróximo(Restaurante[] vector, Punto p){
      int posicion = 0;
      int distancia_menor = vector[0].getDistancia(p);
      for(int i=0; i<vector.length; i++){
         if(vector[i].getDistancia(p) < distancia_menor){
            distancia_menor = vector[i].getDistancia(p);
            posicion = i;
         }
      }
      return vector[posicion];
   }

   public static void main(String[] args) {
      System.out.print("Indique su posición actual");
      Scanner sc = new Scanner(System.in);
      System.out.print("Coordenada X: ");
      Double x = sc.nextDouble();
      System.out.print("Coordenada Y: ");
      Double y = sc.nextDouble();

      Punto p = new Punto(x, y);
      Restaurante[] vector = leeRestaurantes("restaurantes.txt");

      Restaurante cercano = restauranteMásPróximo(vector, p);
      System.out.println("El restaurante más cercano a su posición es " + cercano.getNombre() + ", situado en el punto " + cercano.toString(cercano.getX(), cercano.getY()));
   }*/
}
